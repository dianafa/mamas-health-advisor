export const KnowledgeBaseClient = () => {
  const getIngredientInfo = (ingredient: string) => {
    return {
      // todo: this needs to be enum
      status: 'green',
      description: 'blabla'
    }
  }
}
